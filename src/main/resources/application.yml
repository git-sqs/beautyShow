#数据库连接
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/carsqs?serverTimezone=UTC
    username: root
    password: sqs18236255547
    type: com.alibaba.druid.pool.DruidDataSource

  thymeleaf:
    cache: false
    encoding: utf-8
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML5
    servlet:
      content-type: text/html

# mybatis配置
mybatis:
  mapper-locations: classpath*:/com/mac/mapper/*.xml



