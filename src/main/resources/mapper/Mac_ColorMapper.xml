<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mac.product.dao.Mac_ColorMapper">
  <resultMap id="BaseResultMap" type="com.mac.product.entity.Mac_Color">
    <result column="c_id" jdbcType="INTEGER" property="cId" />
    <result column="c_name" jdbcType="VARCHAR" property="cName" />
  </resultMap>

  <insert id="insert" parameterType="com.mac.product.entity.Mac_Color">
    insert into mac_color (c_id, c_name)
    values (#{cId,jdbcType=INTEGER}, #{cName,jdbcType=VARCHAR})
  </insert>
  <insert id="insertSelective" parameterType="com.mac.product.entity.Mac_Color">
    insert into mac_color
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="cId != null">
        c_id,
      </if>
      <if test="cName != null">
        c_name,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="cId != null">
        #{cId,jdbcType=INTEGER},
      </if>
      <if test="cName != null">
        #{cName,jdbcType=VARCHAR},
      </if>
    </trim>
  </insert>

</mapper>